{
  "modid": "${project.artifactId}",
  "name": "${project.name}",
  "author_list": ["alexdriedger", "erasels"],
  "description": "${project.description}",
  "credits": "Spire Logs and Jorbs for data set the neural network was trained on. Rincewind for ZHS Translation",
  "version": "${project.version}",
  "sts_version": "${SlayTheSpire.version}",
  "mts_version": "${ModTheSpire.version}",
  "dependencies": ["basemod"],
  "update_json": ""
}

